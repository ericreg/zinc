fn main() {
    values = chan()
    values <- 7
    close(values)

    first = <-values
    print("{first}")

    select {
        case value, is_open = <-values {
            print("{value}")
            print("{is_open}")
        }
    }
}
