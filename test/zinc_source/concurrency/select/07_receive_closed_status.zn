fn main() {
    values = chan()
    values <- 7
    close(values)

    first = <-values
    print("{first}")

    select {
        case value, ok = <-values {
            print("{value}")
            print("{ok}")
        }
    }
}
