fn main() {
    scores = sort_dict()
    scores["x"] = 4
    x = scores.get("x")
    print("{x}")

    values = sort_set()
    values.insert(3)
    has_three = values.contains(3)
    print("{has_three}")
}
