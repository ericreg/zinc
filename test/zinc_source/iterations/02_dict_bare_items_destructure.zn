fn main() {
    scores = sortdict()
    scores["b"] = 2
    scores["a"] = 1

    total = 0
    for key, value in scores {
        print("{key}")
        total = total + value
    }
    print("{total}")
}
